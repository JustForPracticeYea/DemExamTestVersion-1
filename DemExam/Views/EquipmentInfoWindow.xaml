<Window x:Class="DemExam.Views.EquipmentInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DemExam.Views"
        mc:Ignorable="d"
        Title="EquipmentInfoWindow" Height="800" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="5">
            <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Left">
                <Button Content="Назад"
                    Width="100"
                    Click="BackButton_Click"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right">
                <Button x:Name="SaveButton"
                    Content="Сохранить"
                    Width="100"
                    Click="SaveEquipmentButton_Click"/>
                <Button x:Name="DeleteButton"
                    Content="Удалить"
                    Width="100"
                    Background="#E32636"
                    Click="ButtonClickDeleteEquipment"
                    Visibility="Collapsed"/>
            </StackPanel>
        </Grid>

        <!-- Основной контент -->
        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1">
                <!-- Название -->
                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBlock Text="Название" Width="100"/>
                    <TextBox x:Name="NameTextBox" Width="300" Text="{Binding NameEquipment}"/>
                </StackPanel>
                <TextBlock x:Name="NameError" Foreground="Red" FontSize="10" Margin="105,0,0,5"/>

                <!-- Инвентарный номер -->
                <StackPanel Orientation="Vertical" Margin="5" HorizontalAlignment="Left">
                    <TextBlock Text="Инвентарный номер" Width="400"/>
                    <TextBox x:Name="InventoryNumberTextBox" Width="400" Text="{Binding InventoryNumber}"/>
                </StackPanel>
                <TextBlock x:Name="InventoryError" Foreground="Red" FontSize="10" Margin="5,0,0,5"/>

                <!-- Вес -->
                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBlock Text="Вес" Width="40"/>
                    <TextBox x:Name="WeightTextBox" Width="300" Text="{Binding Weight}"/>
                </StackPanel>
                <TextBlock x:Name="WeightError" Foreground="Red" FontSize="10" Margin="105,0,0,5"/>

                <!-- Срок службы -->
                <StackPanel Orientation="Vertical" Margin="5" HorizontalAlignment="Left">
                    <TextBlock Text="Срок службы (лет)" Width="400"/>
                    <TextBox x:Name="StandartTimeLimitTextBox" Width="400" Text="{Binding StandartTimeLimit}"/>
                </StackPanel>
                <TextBlock x:Name="TimeLimitError" Foreground="Red" FontSize="10" Margin="5,0,0,5"/>

                <!-- Подразделение -->
                <StackPanel Orientation="Vertical" Margin="5" HorizontalAlignment="Left">
                    <TextBlock Text="Подразделение" Width="400"/>
                    <ComboBox x:Name="OfficeComboBox" 
                          Width="400"
                          DisplayMemberPath="ShortName"
                          SelectedValuePath="Id"
                          SelectionChanged="OfficeComboBoxSelectionChanged"/>
                </StackPanel>
                <TextBlock x:Name="OfficeError" Foreground="Red" FontSize="10" Margin="5,0,0,5"/>

                <!-- Аудитория -->
                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBlock Text="Аудитория" Width="100"/>
                    <ComboBox x:Name="AuditoriumComboBox" 
                          Width="300"
                          DisplayMemberPath="AuditoriumNumber"
                          SelectedValuePath="Id"/>
                </StackPanel>
                <TextBlock x:Name="AuditoriumError" Foreground="Red" FontSize="10" Margin="105,0,0,5"/>

                <!-- Дата поставки -->
                <StackPanel Orientation="Vertical" Margin="5" HorizontalAlignment="Left">
                    <TextBlock Text="Дата поставки" Width="400"/>
                    <DatePicker x:Name="TransferDatePicker" Width="400" SelectedDate="{Binding TransferToCompanyBalanceDate}"/>
                </StackPanel>
                
                <!-- Описание -->
                <StackPanel Orientation="Vertical" Margin="5" HorizontalAlignment="Left">
                    <TextBlock Text="Описание" Width="100" HorizontalAlignment="Left"/>
                    <TextBox x:Name="DescriptionTextBox" Width="300" Height="100" Text="{Binding Description}"/>
                </StackPanel>
            </StackPanel>
            

            <!-- фото -->
            <StackPanel Grid.Column="0">
                <Button x:Name="ChangePhotoButton"
                    Content="Изменить фото"
                    Height="35"
                    Click="ChangePhotoButtonClick"/>
                <Border BorderBrush="Gray"
                    BorderThickness="1"
                    Height="300"
                    Margin="0,10,0,0">
                    <Image x:Name="PhotoImage"
                       Stretch="Uniform"/>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
